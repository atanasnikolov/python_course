# Напишете функция, която приема като параметър списък и връща
# нов списък, който съдържа елементите на подадения списък без
# повторенията.
# Екстра: Напишете две различни функции – една, която използва цикъл;
# и друга, която използва множества.

list = [1,1,2,2,3,3,3,3,3,43,4,4,4,4,4,4,4,3,4,4,5,5,6,6]
def func_list(list):    
    solution = set(list)
    empty = []
    for i in solution:
        empty.append(i)
    return empty

print(func_list(list))